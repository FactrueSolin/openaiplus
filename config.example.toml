# Copy to config.toml for reproducible local settings.
host = "127.0.0.1"
port = 3000

# Leave empty to disable proxy by default.
# Example: PROXY_POOL="http://127.0.0.1:7890,socks5h://127.0.0.1:1080"
proxy_pool = []
